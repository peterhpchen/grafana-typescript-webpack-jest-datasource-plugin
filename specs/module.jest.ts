import {Datasource, QueryCtrl, ConfigCtrl} from '../src/module';

describe('module', () => {
  describe('Datasource', () => {
    it('should exist', () => {
      expect(Datasource).toBeDefined();
    });
  });
  describe('QueryCtrl', () => {
    it('should exist', () => {
      expect(QueryCtrl).toBeDefined();
    });
  });
  describe('ConfigCtrl', () => {
    it('should exist', () => {
      expect(ConfigCtrl).toBeDefined();
    });
  });
});
